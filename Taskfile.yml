version: "3"

tasks:
  format:
    desc: Format source and documentation
    cmds:
      - cargo fmt --all
      - panache format .

  lint:
    desc: Lint source and documentation
    cmds:
      - cargo clippy --locked --all-targets --all-features -- -D warnings
      - panache lint .

  test:
    desc: Run tests
    cmds:
      - cargo test --locked --no-default-features
      - cargo test --locked --all-features

  example:
    desc: Verify dense and sparse normalization and lasso fitting
    cmds:
      - cargo run --locked --example normalization --features faer
      - cargo run --locked --example lasso --features faer

  bench:
    desc: Benchmark Gaussian lasso with only the faer backend enabled
    cmds:
      - cargo bench --locked --no-default-features --features faer --bench lasso

  check:
    desc: Run all required checks
    cmds:
      - cargo fmt --all -- --check
      - cargo clippy --locked --all-targets --all-features -- -D warnings
      - cargo build --locked --no-default-features
      - task: test
      - task: example
      - RUSTDOCFLAGS="-D warnings" cargo doc --locked --no-deps --all-features
      - panache format --check .
      - panache lint .
